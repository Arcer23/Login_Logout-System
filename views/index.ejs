<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resgister</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10 block">
        <h3 class="mb-5 text-3xl">Create Account</h3>
        <form action="/register" method='post'>
            <input type="text" class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="Name" name="name" />
            <input type="text" class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="username" name="username" />
            <input type="password" class="px-3 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="password" name="password" />
            <input type="submit" class="px-3 py-2 outline-none bg-transparent border-2 border-blue-500 rounded-md"
                value="Create Account" />
        </form>
        <div class="mt-4">
            <a class="underline text-blue-500" href="/login">Login To Existing Account ?</a>
        </div>
    </div>
</body>

</html>