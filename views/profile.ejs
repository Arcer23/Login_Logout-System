<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex items-center justify-end">
            <a href="/logout" class="bg-red-600 border-2 border-red-500 p-2 rounded-md text-sm">Logout</a>
        </div>
        <div>
            <h3 class=" text-3xl"><span class="font-light text-slate-500">
                    Hello
                </span>
                <%= user.name %>
            </h3>
        </div>
        <h5 class="mb-2">You can create a new post</h5>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent border-2 border-slate-500 rounded-md outline-none p-2" cols="36"
                placeholder=" What's on Your Mind ?" name="content"></textarea>
            <input type="submit"
                class="px-3 py-2 bg-blue-500 border-blue-500 rounded-md block mt-2 border-2 w-40 text-sm"
                value="Create New Post" />
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-500 text-3xl">Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 border-[1px] border-zinc-500 rounded-md mt-1 bg-zinc-800 mb-2">
                        <h3 class="text-blue-500 mb-1">@<%= user.username %>
                        </h3>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small >
                            <%= post.likes.length %>
                        </small>
                        <div class="btns flex mt-1 gap-4">
                            <a class="text-blue-500" href="/like/<%= post.id %>">
                                <%= post.likes.indexOf(user.userid) === -1 ?"like" : "Unlike" %>

                            </a>
                            <a class="text-zinc-300" href="/edit/<%= post._id%>">Edit</a>
                            <a class="text-red-600" href="">Delete</a>
                        </div>
                    </div>
                    <% }) %>

            </div>
        </div>
    </div>
</body>

</html>