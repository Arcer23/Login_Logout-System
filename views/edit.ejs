<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <h3 class="text-3xl mb-3"> Edit Your Post</h3>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="resize-none bg-transparent border-2 border-slate-500 rounded-md outline-none p-2" cols="36"
                placeholder=" What's on Your Mind ?" name="content">
                <%= post.content %>
            </textarea>
            <input type="submit"
                class="px-3 py-2 bg-blue-500 border-blue-500 rounded-md block mt-2 border-2 w-30 text-sm"
                value="Edit Post" />
        </form>
    </div>
</body>

</html>